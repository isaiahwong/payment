apiVersion: v1
kind: ConfigMap
metadata:
  name: dunamis-env-configmap
  namespace: default
data:
  NODE_ENV: "development"
  PORT: "50051"
  MAINTENANCE_MODE: "false"
  
  # Logging
  ENABLE_STACKDRIVER: "false"
